{{- include "common.configmap" (list . "vertex-containers.configmap") -}}
{{- define "vertex-containers.configmap" -}}
data:
  VERTEX_LOGS_ADDR: $LOGS_SERVICE_HOST:$LOGS_SERVICE_PORT
  VERTEX_AUTH_ADDR: $AUTH_SERVICE_HOST:$AUTH_SERVICE_PORT
  VERTEX_CONTAINERS_KERNEL_ADDR: $CONTAINERS_KERNEL_SERVICE_HOST:$CONTAINERS_KERNEL_SERVICE_PORT
  VERTEX_DB_HOST: {{ .Release.Name }}-postgresql
  VERTEX_DB_PORT: {{ .Values.postgresql.port | quote }}
{{- end -}}
